# установите права на запуск файла install_monitor
chmod +x install_monitor

# Запустите установку сервисов Prometheus node_exporter Grafana
./install_monitor

# Идем на IP:3000/datasources/ и жмем "Add data source", выбираем Prometheus

# Идем на IP:3000/dashboard/import и вводим нужный dashboard ID, жмем Load и наслаждаемся красивыми графиками загрузки нашего сервера...
